📌 Benchmark Rules (must follow)

Skill Names
• Must be stable
• Must not be overly generic (“Performance”, “Quality”, “Frontend”)

Keywords
• Must be unique identifiers (useState, slice, memo)
• Must NOT be generic (state, function, component, render)

Adding a New Skill 1. Add it in benchmark 2. Add dictionary entry (even if empty) 3. Test three cases 4. Commit with PR note “Benchmark update FOLLOWED RULES”
